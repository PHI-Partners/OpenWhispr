# Implementation Backlog — Meeting Recorder & Transcriber

Numbered in implementation order; tick a checkbox when its task is done and verified. Scope confirmed 2026-09-17: tray/hotkey/overlay, Vulkan + CPU fallback, native WASAPI helper and signing + auto-update (GitHub Releases) are all mandatory.

## Architecture reference (shared by all tasks)

- **Spec sources:** ARCHITECTURE.md (§1 IPC, §2 audio, §3 whisper, §4 data model, §5 packaging, §6 build order), TECH_STACK.md, UI_COMPONENTS.md, THEME.md. Feature epics 4 → 12 follow ARCHITECTURE §6 strictly sequentially ("do not build these in parallel").
- **Layout (electron-vite, TypeScript everywhere):** `src/main` (Electron main), `src/preload`, `src/shared` (IPC channel names + payload types), `src/renderer` (React; one HTML entry per window), `resources/` (`bin/cpu`, `bin/vulkan`, `bin/windows-system-audio-helper.exe`, `models/`, `icons/`, helper C source), `scripts/` (Node `.mjs` provisioning/build scripts), `tests/` (`fixtures`, `fakes`, `helpers`, `mocks`, `e2e`).
- **IPC:** `window.api` via `contextBridge`; kebab-case channels; every `ipcMain.handle` registered centrally in `src/main/ipc/ipcHandlers.ts` with injected services; the five v1 methods of ARCHITECTURE §1 keep their exact names/channels; every `on*` subscription returns an unsubscribe function.
- **Audio pipeline:** capture-host renderer (the main window) → one `MediaRecorder` (webm/opus, 1 s timeslice) → sequence-numbered `meeting-audio-chunk` IPC → main → one long-running `ffmpeg-static` process → 16 kHz mono s16le PCM → tee into (a) incremental WAV writer (`<userData>/recordings/*.wav` = the meeting's `audio_path`) and (b) silence-aware chunker → serial transcription queue → whisper-server `/inference` → `transcript-update` event + DB append.
- **System audio strategy** chosen at recording start: Electron loopback (`desktopCapturer` source granted through `session.setDisplayMediaRequestHandler` with `audio: 'loopback'`, captured and mixed in the renderer — Epic 6; permanent fallback), or native WASAPI helper PCM mixed in main (Epic 12; preferred when `probe` succeeds). The legacy `chromeMediaSource: 'desktop'` snippet in ARCHITECTURE §2 is not used: it crashes the renderer on current Electron.
- **Recording state:** one state machine in main (`idle → starting → recording → finalizing → idle | error`) broadcast to all windows on `recording-state-changed`; every trigger (window button, overlay, tray, hotkey) goes through it.
- **whisper-server:** spawned once at app start, first free port in 8178–8199, `--model <path> --host 127.0.0.1 --port <port>`, readiness and 5 s health check on `GET /health`, restart with backoff. Epic 11 swaps the CPU package for a Vulkan-enabled build (dynamic ggml backends, all CPU variants incl. AVX2 in the same directory) and falls back to CPU by respawning with `--no-gpu`.
- **Data:** `better-sqlite3`, the single `meetings` table exactly as in ARCHITECTURE §4, schema evolution only through additive `ALTER TABLE … ADD COLUMN` in try/catch.
- **Test seams:** environment overrides read only in `src/main/config.ts` — `OW_USER_DATA_DIR`, `OW_WHISPER_SERVER_CMD`, `OW_AUDIO_HELPER_CMD`, `OW_CHUNK_SECONDS`, `OW_FORCE_CPU`, `OW_DISABLE_SYSTEM_AUDIO`, `OW_DISABLE_UPDATER`.
- **Version compatibility (validated 2026-09-17 — re-check through Context7/npm when installing, never blindly take `@latest`):** Electron 44.x; electron-vite 5.x supports Vite ≤ 7 (not 8) with the matching `@vitejs/plugin-react` 5.x; Vitest 5 (`test.projects`); `better-sqlite3` ≥ 13 ships an N-API prebuild that loads in both Node and Electron (no native rebuild); electron-builder 26.x key names (`win.signtoolOptions`, `forceCodeSigning`); whisper.cpp Windows binaries live on `bNNNN` release tags and there is no official Vulkan build.
- **Definition of done for every task:** tests written first (TDD) with the Epic 2 harness; `npm run verify` green; real-binary smoke run when the task touches real binaries; library APIs checked through Context7; app-level behaviour verified with Playwright (per `CLAUDE.md`).

## Backlog

- [ ] **1. Epic / Foundation: Project scaffold and toolchain**
  - [ ] **1.1 Story: Runnable three-process Electron + React + TypeScript workspace**
    - [x] **1.1.1 Task: Initialise the repository and electron-vite workspace.** `git init`; `.gitignore` (`node_modules`, `out`, `dist`, `resources/bin`, `resources/models`, test reports, logs); `package.json` (`main: out/main/index.js`, scripts `dev` / `build` / `preview`, exact mutually compatible version pins per the architecture reference, Electron binary install step if the pinned Electron no longer downloads on postinstall); strict TypeScript configs (node: main + preload + shared + scripts; web: renderer + shared); `electron.vite.config.ts` with entries `src/main/index.ts`, `src/preload/index.ts`, `src/renderer/index.html` and aliases `@shared` / `@` (renderer); runtime packages that must stay external (`better-sqlite3`, `ffmpeg-static`, `electron-updater`) live in `dependencies`; React 19 root with `@vitejs/plugin-react`; Tailwind v4 through `@tailwindcss/vite` with a stub `index.css`; install the documented core dependencies (UI_COMPONENTS.md "What to reuse": `zustand`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `@tanstack/react-virtual`) — Radix primitives arrive with the shadcn components that use them (8.1.2). Outcome: `npm run dev` opens a window rendering React with HMR; `npm run build` emits `out/{main,preload,renderer}`.
    - [x] **1.1.2 Task: Secure window bootstrap and typed IPC bridge skeleton.** `src/shared/ipc.ts` with kebab-case channel constants, payload types and the `Api` interface containing the five v1 methods of ARCHITECTURE §1 (`startMeetingRecording`, `stopMeetingRecording`, `onTranscriptUpdate`, `listMeetings`, `getMeeting`); bundled sandbox-safe preload exposes `window.api` through `contextBridge` (global typing for the renderer; `on*` returns unsubscribe); main window created with `contextIsolation: true`, `sandbox: true`, `nodeIntegration: false`, `backgroundThrottling: false`, no experimental web features; `src/main/ipc/ipcHandlers.ts` exporting `registerIpcHandlers(deps)` as the single registration point (handlers are added by later tasks). Outcome: renderer can call a typed `window.api` and TypeScript fails when preload and `Api` drift.
    - [x] **1.1.3 Task: Code-quality tooling and initial developer docs.** ESLint (typescript-eslint, react-hooks) + Prettier; scripts `typecheck` (both tsconfigs), `lint`, `format`; fill the `CLAUDE.md` sections already knowable (Prerequisites, Quick Start: Install and Build, Tech Stack, Folder Structure) — remaining sections are completed in 13.2.1.

- [ ] **2. Epic / Foundation: Testing harness and verification loop**
  - [ ] **2.1 Story: Unit and integration harness (Vitest)**
    - [ ] **2.1.1 Task: Vitest workspace with `main` and `renderer` projects.** `test.projects`: `main` (node environment for `src/main`, `src/shared`, `scripts`) and `renderer` (jsdom + React Testing Library + `@testing-library/jest-dom` + `user-event`). Scripts `test`, `test:main`, `test:renderer`, `test:watch`, v8 coverage. Proof tests: one per project; the `main` one opens an in-memory `better-sqlite3` database, guarding the assumption that the same N-API binary loads under Node and Electron (if the installed version turns out not to be N-API, run the `main` project under `ELECTRON_RUN_AS_NODE` instead).
    - [ ] **2.1.2 Task: Main-process test kit — Electron mock, helpers, fake whisper-server, fixtures.** `tests/mocks/electron.ts` (fake `app` paths/`isPackaged`, `ipcMain` with invoke simulation and sender frames, `BrowserWindow`/`webContents.send` recorder, `Tray`, `Menu`, `globalShortcut`, `session`, `desktopCapturer`); `tests/helpers` (`makeTempDir` with cleanup, PCM/WAV generators for tone/silence, `parseWavHeader`, `rms`, `waitFor`); `tests/fakes/fake-whisper-server.mjs` — CLI-compatible with the real binary (`--model --host --port --no-gpu`), `GET /health`, `POST /inference` that parses multipart, asserts 16 kHz mono WAV and returns scripted `{ text }`, scripted startup log lines, switchable failure modes (slow start, never ready, crash after N requests, HTTP 500 `{ error }`, hang, simulated GPU-init failure) and a request log file for assertions; `scripts/fetch-test-fixtures.mjs` producing committed fixtures `tests/fixtures/jfk.wav` (whisper.cpp sample, SHA-256 pinned), `jfk.webm` (opus, generated with `ffmpeg-static`), `tone.wav`, `silence.wav`. Tests: the fake server's own behaviour modes.
    - [ ] **2.1.3 Task: Renderer test kit.** `createMockApi()` — a full typed implementation of `Api` with controllable event emitters (compile-time lockstep with the contract); fakes for `navigator.mediaDevices` (`getUserMedia`, `getDisplayMedia`), `MediaStream`/`MediaStreamTrack`, `MediaRecorder` (timeslice `dataavailable` under fake timers) and an `AudioContext` graph that records node connections; `renderWithApp()` helper that installs the mock API and resets Zustand stores between tests.
  - [ ] **2.2 Story: End-to-end harness (Playwright + Electron)**
    - [ ] **2.2.1 Task: Playwright Electron launch fixture and smoke spec.** `playwright.config.ts` + `tests/e2e/fixtures.ts` exposing `app` / `mainWindow`: launches the built app via `_electron.launch` with Chromium fake-media switches placed before the main script path (`--use-fake-device-for-media-stream`, `--use-file-for-fake-audio-capture=tests/fixtures/jfk.wav`), an isolated temp `OW_USER_DATA_DIR`, `OW_WHISPER_SERVER_CMD` pointing at the fake server, short `OW_CHUNK_SECONDS`, `OW_DISABLE_UPDATER=1`, trace + screenshot on failure, guaranteed app/child-process teardown. Script `test:e2e` (build, then run). Smoke spec: app launches, main window visible, `window.api` exposed, no console errors.
    - [ ] **2.2.2 Task: Browser-preview mode for Playwright-driven UI verification.** DEV-only (tree-shaken from production via `import.meta.env.DEV`): when the renderer runs in a plain browser without `window.api`, install an in-memory mock API with seeded meetings and a scripted recording/transcript simulation, so screens can be inspected and screenshotted through the Playwright CLI/MCP tools in light and dark mode without Electron. Document the loop in `CLAUDE.md`.
    - [ ] **2.2.3 Task: Aggregate regression gate and real-binary smoke project.** `npm run verify` = typecheck + lint + Vitest (both projects) + E2E; separate opt-in Playwright project `test:smoke:real` that uses the real whisper-server/model (and later the real helper and packaged build), auto-skipping with a clear message when binaries are absent — real-smoke specs are added by Epics 5, 6, 10, 11, 12.

- [ ] **3. Epic / Foundation: Core main-process infrastructure**
  - [ ] **3.1 Story: Configuration, paths and logging**
    - [ ] **3.1.1 Task: Central config and path resolution.** `src/main/config.ts`: documented constants (whisper port range 8178–8199, health interval 5 s, sample rate 16 000, model `ggml-base.en.bin`, chunk target/tolerance, default hotkey accelerator), resource resolution for dev (`<repo>/resources`) vs packaged (`process.resourcesPath`), user-data subfolders (`recordings/`, `logs/`, database file), and the `OW_*` overrides as the only test seams. Tests: dev/packaged resolution, override precedence, invalid override values rejected.
    - [ ] **3.1.2 Task: Logging and error capture.** Small in-house leveled logger (timestamped lines to console + size-capped rotating file in `logs/`), child-process output adapter (used for ffmpeg, whisper-server, audio helper), `uncaughtException` / `unhandledRejection` capture, and `log-renderer-error` channel forwarding renderer `error` / `unhandledrejection` events. Tests: rotation, level filtering, adapter line splitting, renderer forwarding.
  - [ ] **3.2 Story: IPC hardening, window security and permissions**
    - [ ] **3.2.1 Task: Validated IPC handler layer.** `handle(channel, validator, fn)` wrapper in `ipcHandlers.ts`: rejects calls whose `senderFrame` is not the app's own origin, validates arguments (types, integer ids, chunk size ceiling), logs failures and rethrows serialisable errors. Contract test: every channel constant in `src/shared/ipc.ts` has exactly one registered handler or emitter and a matching preload method.
    - [ ] **3.2.2 Task: Window security and media permissions.** Content-Security-Policy for the renderer, `will-navigate` / `setWindowOpenHandler` lock-down, `session.setPermissionRequestHandler` + `setPermissionCheckHandler` granting only audio `media` to the app's own origin and denying everything else (Epic 6 adds `display-capture`). Tests: permission matrix with the Electron mock; E2E asserts `getUserMedia` resolves and an external navigation is blocked.

- [ ] **4. Epic: Microphone recording to WAV (ARCHITECTURE §6 step 1)**
  - [ ] **4.1 Story: Main-side audio ingest**
    - [ ] **4.1.1 Task: Streaming ffmpeg decoder.** `src/main/audio/ffmpegDecoder.ts`: spawns `ffmpeg-static` (`windowsHide`, path resolver aware of `app.asar.unpacked`), reads MediaRecorder output on stdin as `-f matroska` with low-latency probe flags, resamples with `aresample=16000:async=1:first_pts=0` (timestamp gaps become silence so the PCM timeline tracks wall-clock time), emits continuous mono s16le PCM (carrying odd trailing bytes between reads); stops only by ending stdin and awaiting `close` — never by killing; stderr to logger; typed errors for spawn failure, non-zero exit, `EPIPE`, write-after-end. Tests: stream `jfk.webm` in 1 s slices → PCM byte count ≈ duration × 32 000; graceful end flushes the tail; abort mid-stream; missing binary.
    - [ ] **4.1.2 Task: Incremental WAV writer with repair.** Writes a RIFF header placeholder, appends PCM as it arrives, patches sizes on close; `repairWavHeader(path)` fixes files left by a crash; reports duration from byte count. Tests: header correctness, large-write streaming, repair of a truncated file.
    - [ ] **4.1.3 Task: Recording session and main-owned state machine.** `RecordingSession` (decoder → WAV writer, event emitter `started` / `pcm` / `completed{audioPath,durationSeconds}` / `failed`; rejects out-of-order chunk sequence numbers) and `RecordingController` (`idle → starting → recording → finalizing → idle | error`, single-active-session guard, timeout if no audio arrives). Channels: `meeting-recording-start` (returns `{ sessionId, captureSystemAudio }`), `meeting-audio-chunk` (`{ seq, data }`), `meeting-recording-stop` (resolves after finalisation), `recording-get-state`, broadcast `recording-state-changed`. Tests: legal/illegal transitions, double start, stop flush ordering (final chunk → stdin end → decoder close → WAV finalised), decoder failure → `error` with WAV still finalised.
  - [ ] **4.2 Story: Renderer microphone capture**
    - [ ] **4.2.1 Task: Capture service, recording store and minimal record control.** `AudioSourceProvider` interface (injected) with production `getMicStream()` via `getUserMedia({ audio: true })`; `MediaRecorder` (webm/opus, 1 s timeslice) whose blobs are converted and forwarded strictly in order with sequence numbers through `sendAudioChunk`; stop sequence awaits the final `dataavailable` before `stopMeetingRecording`; tracks released on every exit path; error mapping (`NotAllowedError` → guidance + button opening `ms-settings:privacy-microphone` via an `app-open-mic-settings` channel, `NotFoundError` → no microphone, generic fallback); Zustand `useRecordingStore` mirroring main state; bare Record/Stop button, elapsed timer and error text (styled later in Epic 8). Tests: RTL with media fakes for happy path, denial, no device, chunk ordering under slow blob conversion, stop ordering, cleanup.
    - [ ] **4.2.2 Task: E2E — record to a playable WAV.** Record ~3 s with the fake microphone file, stop, then assert a WAV exists under the temp user-data dir with a valid RIFF header, 16 kHz mono 16-bit, duration within tolerance and non-silent RMS.

- [ ] **5. Epic: Local transcription through whisper-server (ARCHITECTURE §6 step 2)**
  - [ ] **5.1 Story: Binary and model provisioning**
    - [ ] **5.1.1 Task: `npm run setup:whisper` provisioning script.** `scripts/fetch-whisper.mjs`: downloads `whisper-bin-x64.zip` from an explicitly pinned whisper.cpp `bNNNN` release tag (never `releases/latest`, which carries no assets) into `resources/bin/cpu/` — `whisper-server.exe`, `whisper.dll`, `ggml.dll`, `ggml-base.dll`, the `ggml-cpu-*.dll` variant set — plus the MSVC runtime DLLs the zip omits (`msvcp140.dll`, `vcruntime140.dll`, `vcruntime140_1.dll`, `vcomp140.dll`, copied from the local VC++ redistributable) so the exe starts on a clean machine; downloads `ggml-base.en.bin` from Hugging Face into `resources/models/`; SHA-256 verification of every download, idempotent, actionable failures. Config resolves binary/model paths; a startup preflight reports missing files as a typed error. Tests: checksum mismatch, idempotency, preflight (HTTP mocked).
  - [ ] **5.2 Story: Server lifecycle**
    - [ ] **5.2.1 Task: `WhisperServerManager`.** First free port in 8178–8199; spawn with `--model --host 127.0.0.1 --port`; readiness = poll `GET /health` until 200 (connection refused while the model loads) with timeout; 5 s health check; unhealthy/crash → restart with bounded exponential backoff; state events `starting | ready | unhealthy | stopped | failed`; output to logger; started once at app ready and stopped on quit (`before-quit` plus process-exit safety kill). Errors: binary/model missing, port range exhausted or bind failure (exit code 1), readiness timeout. Tests against the fake server: occupied ports, slow start, never ready, crash + restart, clean stop leaves no process.
  - [ ] **5.3 Story: Chunked live transcription**
    - [ ] **5.3.1 Task: `WhisperClient`.** Wraps a PCM window in a WAV buffer and POSTs multipart to `/inference` (`file`, `response_format=json`, `temperature=0`) with native `fetch`/`FormData`; request timeout; typed errors from HTTP 400/500 `{ error }` bodies; text normalisation (trim, collapse whitespace, strip non-speech markers such as `[BLANK_AUDIO]`). Tests with the fake server: success, 500, timeout, malformed JSON, marker stripping.
    - [ ] **5.3.2 Task: Silence-aware PCM chunker and serial transcription queue.** Chunker: target window `OW_CHUNK_SECONDS` (default 30 s), cut at the lowest-energy frame inside a tolerance band before the hard maximum so words are not split, flush remainder on end, drop windows below an RMS silence threshold, emit `{ seq, startSec, endSec, pcm }`. Queue: strictly ordered, one in flight (the server serialises inference anyway), waits for server `ready`, retries a window after server restart, bounded retries then records a gap marker, `drain()` for stop. Tests: synthetic PCM cut-point selection, ordering under slow responses, retry across restart, drain semantics.
    - [ ] **5.3.3 Task: Live transcript wiring and display.** Tee session PCM into the chunker/queue; emit `transcript-update` `{ sessionId, seq, text, startSec, endSec }`; `finalizing` state lasts until the queue drains; whisper-unavailable surfaced as a recoverable warning (audio keeps recording, windows queue up). Renderer: append-only live transcript list with auto-scroll and "finishing transcription…" state. Tests: integration (`jfk.webm` + fake server → ordered updates, stop waits for drain); E2E with scripted fake text appearing live; real smoke (`test:smoke:real`): fake mic = `jfk.wav` → transcript contains "ask not what your country can do for you", and time-to-first-PCM is logged to validate the ffmpeg probe flags.

- [ ] **6. Epic: System audio through Electron loopback and mixing (ARCHITECTURE §6 step 3)**
  - [ ] **6.1 Story: Capture "what you hear" and mix with the microphone**
    - [ ] **6.1.1 Task: Loopback capture via `desktopCapturer`.** Main registers one `session.setDisplayMediaRequestHandler` (try/catch-guarded) that serves only the app's own frame: `desktopCapturer.getSources({ types: ['screen'], thumbnailSize: 0×0 })` → `callback({ video: sources[0], audio: 'loopback' })`, denying all other requesters; permission handlers additionally grant `display-capture` to the app origin. Renderer `getSystemStream()` calls `getDisplayMedia({ video: true, audio: true })` (video is mandatory) and immediately stops/removes the video track. Capture must start without a page user gesture (tray/hotkey starts arrive over IPC): covered by an E2E that starts recording from the main process with no click; if an Electron upgrade ever enforces activation, trigger capture through `webContents.executeJavaScript(code, true)`. `OW_DISABLE_SYSTEM_AUDIO` forces the failure path for tests. Tests: handler grant/deny matrix with the Electron mock; renderer provider with media fakes.
    - [ ] **6.1.2 Task: Web Audio mixing graph with graceful degradation.** `AudioContext` graph: mic source + system source → gains → `MediaStreamAudioDestinationNode` → the single `MediaRecorder` (mono mix per ARCHITECTURE "Mixing"). System-audio failure (incl. machines without an output device) or track `ended` → continue mic-only with a `systemAudioUnavailable` warning in state; mic track `ended` → stop and finalise with an error notice; all nodes/tracks/context closed on stop. Tests: graph wiring, degradation and cleanup with fakes; E2E: recording succeeds with loopback enabled, and with `OW_DISABLE_SYSTEM_AUDIO=1` the warning is visible while mic-only recording completes.
    - [ ] **6.1.3 Task: Real both-sides verification.** Real-smoke spec: fake mic plays `tone.wav` while an external process (PowerShell `System.Media.SoundPlayer`) plays `jfk.wav` through the default output device; assert the real whisper transcript contains the JFK phrase, proving loopback audio reaches the mix. Add the manual "real call" check from ARCHITECTURE §6 step 3 to the release checklist (13.2.2).

- [ ] **7. Epic: Meeting persistence (ARCHITECTURE §6 step 4, data half)**
  - [ ] **7.1 Story: SQLite data layer**
    - [ ] **7.1.1 Task: Database module and meeting repository.** `better-sqlite3` (kept external to the bundle, no native rebuild step); database opened under user data with WAL; `CREATE TABLE IF NOT EXISTS meetings` exactly as ARCHITECTURE §4; `addColumnIfMissing()` implementing the additive `ALTER TABLE` try/catch pattern (swallows only "duplicate column"); `MeetingRepository`: `create()`, `appendTranscript(id, text)` (newline-separated paragraphs), `finalize(id, { audioPath, durationSeconds })`, `list()` (id, title, created_at, duration_seconds, transcript preview; newest first), `get(id)`, `findUnfinalized()`; injected `Database` handle. Tests (temp DB files): idempotent schema on reopen, additive-column helper, CRUD, ordering, preview truncation.
    - [ ] **7.1.2 Task: Persist the recording lifecycle and expose meetings over IPC.** Session start creates the row (start result and `transcript-update` gain `meetingId`), each transcript window is appended, stop finalises `audio_path` + `duration_seconds`; handlers `db-list-meetings` and `db-get-meeting` (integer id validation, not-found error); broadcast `meetings-changed`; startup recovery: meetings with `duration_seconds IS NULL` get their WAV header repaired and are finalised from file size. Tests: integration (fixture audio + fake whisper → row content), recovery after simulated crash, IPC validation; E2E: recorded meeting survives an app restart.

- [ ] **8. Epic: UI shell and design system (ARCHITECTURE §6 step 4, UI half)**
  - [ ] **8.1 Story: Theme and component foundation**
    - [ ] **8.1.1 Task: Tailwind v4 `@theme` tokens from THEME.md.** Brand (`--color-primary #4577e9`, accent `oklch(0.55 0.22 285)`, brand glass gradient `#5588F0 → #3466E2`), light hex palette with `surface-0…surface-raised` and border steps, dark `.dark` OKLCH charcoal palette (base `oklch(0.22 0.006 260)`, ~0.05 lightness steps, foreground `oklch(0.93 0 0)`), semantic colours with `-foreground` pairs, radius scale (4/6/8/10/12 px shell), shadow scale (`card`, `card-hover`, `elevated`, `modal`, `glass`), liquid-glass utility, Inter Variable (`@fontsource-variable/inter`) + system stack, motion conventions (200 ms ease-out, `active:scale-[0.985]`), `prefers-reduced-motion` rule stripping transforms but keeping opacity/colour transitions; `.dark` on `<html>` follows the OS theme with no JS theme branching in components; OpenWhispr-only tokens without a consumer here (parchment palette, agent-brand colour, Yowza/Caveat faces) are omitted. Tests: unit test of the OS-theme hook; Playwright light/dark screenshots in browser-preview mode.
    - [ ] **8.1.2 Task: shadcn/ui setup and owned primitives.** Hand-written `components.json` (the shadcn CLI cannot detect `electron.vite.config.ts`): style `new-york`, base colour `neutral`, `lucide-react` icons, empty Tailwind config path, `@/*` alias mirrored in tsconfig and `renderer.resolve.alias`; `cn()` from `clsx` + `tailwind-merge`; generated and restyled primitives in `src/renderer/src/components/ui/`: `Button` (pill `rounded-full`, cva variants incl. brand-glass primary and circular icon size), `Card`, `Dialog`; further primitives from the documented Radix set are generated only by the task that first needs them. Tests: variant class output, accessibility roles/focus ring, keyboard activation.
  - [ ] **8.2 Story: Application screens and workflows**
    - [ ] **8.2.1 Task: App shell and virtualised meetings list.** Inset shell container (`--radius-shell`), sidebar list built with `@tanstack/react-virtual` showing title, date, duration and transcript preview; `useMeetingsStore` (load through `listMeetings`, refresh on `meetings-changed`, selection); loading, empty and error states. Tests: store logic, list rendering/selection with `createMockApi`, virtualisation with 5 000 seeded rows renders a bounded DOM.
    - [ ] **8.2.2 Task: Meeting detail and transcript view.** Loads `getMeeting(id)`; header (title, date, duration); virtualised transcript paragraphs; empty-transcript and not-found states; stale-response guard when selection changes quickly. Tests: component tests for all states and race handling.
    - [ ] **8.2.3 Task: Recording experience.** Brand-glass circular record/stop control, state-driven UI (`starting`, `recording` with elapsed timer, `finalizing` with progress copy), live transcript pane with auto-scroll that pauses when the user scrolls up, warning banners (mic-only degraded, transcription engine restarting), error banners with actionable guidance, automatic selection of the saved meeting after stop. Tests: component tests per state and transition; reduced-motion snapshot.
    - [ ] **8.2.4 Task: E2E workflow suite for the UI.** Record → live transcript → stop → meeting appears first in list → open → transcript shown → restart app → still present; seeded database with thousands of meetings scrolls smoothly; light/dark screenshot assertions of the main states.

- [ ] **9. Epic: Desktop shell — tray, global hotkey, always-on-top overlay**
  - [ ] **9.1 Story: Background-capable application lifecycle**
    - [ ] **9.1.1 Task: Lifecycle and main-routed recording commands.** Single-instance lock (second launch focuses the main window); closing the main window hides it (capture host stays alive); `recording-request-toggle` channel and internal `toggleRecording()` that forwards `recording-toggle-requested` to the capture host so button, overlay, tray and hotkey share one code path; `before-quit` while recording stops and finalises before exiting. Tests: controller routing with the Electron mock; E2E: hide main window → toggle from main process (no page gesture) → recording with system audio runs and finalises.
    - [ ] **9.1.2 Task: Tray icon and menu.** Multi-resolution `.ico` assets (16/20/24/32 px) in `resources/icons` with idle/recording variants; `Tray` instance held at module scope; context menu Show/Hide window, Start/Stop Recording (label and enabled state follow the state machine), Quit; tooltip reflects state; left-click toggles window visibility. Tests: menu template per state; E2E via `electronApp.evaluate` invoking menu item handlers.
    - [ ] **9.1.3 Task: Global hotkey.** `globalShortcut.register` of the config default (`Control+Alt+M`) after app ready, `unregisterAll()` on `will-quit`, debounce of key repeat; a `false` return (accelerator owned by another app — no exception is thrown) is logged and surfaced as a dismissible notice in the main window. Tests: success/failure paths with the mock; E2E triggering the registered callback.
    - [ ] **9.1.4 Task: Always-on-top overlay widget.** Second `BrowserWindow` (`transparent`, `frame: false`, non-resizable, `skipTaskbar`, `setAlwaysOnTop(true, 'screen-saver')`, same preload) loading its own `overlay.html` renderer entry (small bundle); drag region with `no-drag` controls; glass-styled panel using a translucent fill + rim highlight (CSS `backdrop-filter` cannot blur the desktop behind a transparent window) showing recording state, elapsed timer, brand-glass toggle button and "open main window" action; state purely from `recording-state-changed`. Tests: overlay component states; E2E: both windows reflect a toggle, overlay `isAlwaysOnTop()` is true, overlay toggle starts/stops recording while the main window is hidden.

- [ ] **10. Epic: Packaging, code signing and auto-update (ARCHITECTURE §6 step 5)**
  - [ ] **10.1 Story: Installable Windows build**
    - [ ] **10.1.1 Task: electron-builder configuration.** NSIS x64 target, `appId`/`productName`/icons, `files: out/**`, `extraResources` mapping `resources/bin → bin` and `resources/models → models` (never inside asar), `asarUnpack` for `**/*.node` and `node_modules/ffmpeg-static/**`, `npmRebuild: false`, clean `npm ci` before packaging, pre-package check failing the build when whisper binaries, runtime DLLs or the model are missing, scripts `dist` and `dist:dir`. Tests: unit tests of packaged-path resolution (`app.asar` → `app.asar.unpacked`); config test asserting required resources are declared.
    - [ ] **10.1.2 Task: Packaged-app smoke suite.** Real-smoke Playwright project launching `dist/win-unpacked/*.exe` (the `EnableNodeCliInspectArguments` fuse stays enabled so Playwright can attach) with a temp user-data dir, fake mic and `OW_DISABLE_UPDATER=1`: app starts, bundled whisper-server spawns from `process.resourcesPath`, recording is transcribed by the real model, meeting row is stored, tray/overlay exist. Add the clean-machine install procedure (ARCHITECTURE §6 step 5) to the release checklist.
  - [ ] **10.2 Story: Trusted distribution**
    - [ ] **10.2.1 Task: Code signing.** electron-builder 26 Windows signing driven by environment (`WIN_CSC_LINK` / `WIN_CSC_KEY_PASSWORD`, options under `win.signtoolOptions`); dev builds may be unsigned, the release config sets `forceCodeSigning: true` so `npm run release` fails without credentials; bundled executables (whisper-server, later the audio helper) are signed too; post-build verification script using `Get-AuthenticodeSignature` on the installer and bundled executables.
    - [ ] **10.2.2 Task: Auto-update through GitHub Releases.** `publish` provider `github` with explicit `owner`/`repo` (the GitHub repository must exist — obtain it from the user when starting this task); `src/main/updater.ts` with `electron-updater`: check on startup when packaged and `OW_DISABLE_UPDATER` is unset, background download, `update-state-changed` broadcast, in-app "Restart to update" banner + tray menu item, `update-install-now` channel calling `quitAndInstall` — never during `starting`/`recording`/`finalizing`; failures logged, never fatal; `npm run release` builds, signs and publishes with `GH_TOKEN`. Tests: updater state machine with mocked `autoUpdater` events, deferral while recording, banner component.

- [ ] **11. Epic: GPU acceleration — Vulkan build with CPU fallback (ARCHITECTURE §6 step 6)**
  - [ ] **11.1 Story: AMD GPU transcription**
    - [ ] **11.1.1 Task: Vulkan whisper-server build and bundling.** `scripts/build-whisper-vulkan.mjs` (no official Vulkan binary exists): checks prerequisites (VS 2022 Build Tools C++ workload, CMake, LunarG Vulkan SDK with `VULKAN_SDK`), builds whisper.cpp at the pinned tag with `-DGGML_VULKAN=ON -DGGML_BACKEND_DL=ON -DGGML_CPU_ALL_VARIANTS=ON -DGGML_NATIVE=OFF` (target `whisper-server`), copies the exe, `whisper.dll`, `ggml*.dll` incl. `ggml-vulkan.dll` and the CPU variant set (AVX2 = haswell), plus the MSVC runtime DLLs to `resources/bin/vulkan/`; config prefers `bin/vulkan` when present, else `bin/cpu`; release packaging and the pre-package check require the Vulkan build; prerequisites documented.
    - [ ] **11.1.2 Task: Backend selection with automatic fallback.** `WhisperServerManager` starts the Vulkan build and classifies the active backend from startup-log markers (`whisper_backend_init_gpu:` "using Vulkan…" vs "no GPU found"/backend load failure — verified against the pinned build); GPU-init crash, readiness timeout or repeated inference failure on GPU → respawn the same binary with `--no-gpu` for the rest of the session; `OW_FORCE_CPU` override; selected backend and fallback reason written to the log (asserted from the log file in smoke tests). Tests with the fake server simulating each Vulkan failure mode; real smoke on the AMD machine: `jfk.wav` transcribed with the Vulkan backend reported, and with `OW_FORCE_CPU=1` reported as CPU.
    - [ ] **11.1.3 Task: Packaged verification of both backends.** Extend the packaged-app smoke suite to assert Vulkan selection and `--no-gpu` fallback from the installed layout.

- [ ] **12. Epic: Native WASAPI process-loopback helper (ARCHITECTURE §2, after step 6)**
  - [ ] **12.1 Story: Helper executable**
    - [ ] **12.1.1 Task: `resources/windows-system-audio-helper.c`.** Plain-C WASAPI process loopback in exclude-process-tree mode (`VAD\Process_Loopback` via `ActivateAudioInterfaceAsync`; completion handler answering `IUnknown` / `IAgileObject` / `IActivateAudioInterfaceCompletionHandler`, activation wait with timeout); self-supplied `WAVEFORMATEX` with `AUTOCONVERTPCM | SRC_DEFAULT_QUALITY` for the requested rate; CLI protocol exactly as documented: `probe` → one JSON capability line produced by attempting a real activation (not a build-number check); `start --exclude-pid <pid> --sample-rate <hz>` → raw 16-bit mono PCM on binary-mode stdout, line-delimited JSON events on stderr (`start`, `warning`, `error`); continuous timeline (silence synthesised against the performance counter when no packets arrive, `AUDCLNT_BUFFERFLAGS_SILENT` honoured); clean exit when stdin closes; distinct exit codes.
    - [ ] **12.1.2 Task: Helper build script and real-binary tests.** `scripts/build-audio-helper.mjs` locating MSVC and running `cl /O2 windows-system-audio-helper.c /Fe:windows-system-audio-helper.exe ole32.lib mmdevapi.lib` into `resources/bin/`; real-smoke tests: `probe` JSON shape, 2 s capture yields ≈ 2 s of PCM at the requested rate even during silence, external `jfk.wav` playback is audible (RMS) in the capture, exit on stdin close within timeout, bad arguments produce an `error` event.
  - [ ] **12.2 Story: Integration into the recording pipeline**
    - [ ] **12.2.1 Task: `WindowsLoopbackAudioManager` and fake helper.** `src/main/audio/windowsLoopbackAudioManager.ts`: cached `probe`, spawn with the app's pid and 16 000 Hz, stderr JSON-line parser → typed events, PCM stream, stop by closing stdin with kill timeout; `tests/fakes/fake-audio-helper.mjs` speaking the same protocol (scripted PCM, warnings, errors, unsupported probe), injected through `OW_AUDIO_HELPER_CMD`. Tests: probe caching, event parsing with split lines, stop/kill, error propagation.
    - [ ] **12.2.2 Task: Source selection and main-side PCM mixing.** At recording start: helper when `probe` reports support (start result `captureSystemAudio: false` → renderer records mic only), otherwise the Epic 6 renderer loopback (automatic fallback, incl. older Windows); `PcmMixer` between decoder and the WAV/chunker tee: on each arrival mixes up to the shorter of the two buffers with a saturating sum, pads a side with silence once it has stalled beyond a threshold (mic PCM arrives in ≥ 1 s bursts), bounds buffer growth; helper error mid-recording → warning and mic-only continuation. Tests: mixer arithmetic, stall padding on either side, bounded buffers, selection matrix, mid-stream failure; E2E with the fake helper; real smoke: both-sides transcript using the real helper.
    - [ ] **12.2.3 Task: Package the helper.** Add the executable to `extraResources` and the pre-package check, sign it with the other binaries, extend the packaged-app smoke suite to assert the helper path is selected on supported Windows builds.

- [ ] **13. Epic: Resilience validation, documentation and release**
  - [ ] **13.1 Story: Failure-injection and endurance validation**
    - [ ] **13.1.1 Task: Failure-injection E2E suite.** whisper-server crash mid-recording (windows retried, transcript complete and ordered), whisper never ready (recording still saved, clear error), port range exhausted, model file missing, microphone denied / absent, system audio unavailable, ffmpeg failure, unwritable recordings folder, quit during recording — each asserts user-visible messaging, persisted data integrity and log output; fix any defects found at their root.
    - [ ] **13.1.2 Task: Endurance run.** 30-minute accelerated recording against the fake server: bounded memory in main and renderer, queue drains, WAV size equals expected byte count, transcript window count/order correct, no orphan child processes after quit.
  - [ ] **13.2 Story: Documentation and release**
    - [ ] **13.2.1 Task: Complete project documentation.** Fill every remaining `CLAUDE.md` section (Overview, Architecture → Frontend / Backend, Communication Flow with the final channel list, App Configuration with all constants and `OW_*` overrides), toolchain prerequisites for the Vulkan and helper builds, test/verification loop, troubleshooting (logs location, microphone privacy settings, GPU fallback).
    - [ ] **13.2.2 Task: Release candidate and update verification.** Release checklist document; full `npm run verify` + all real-smoke projects green; signed build published to GitHub Releases; on a clean Windows machine: install, record a real call (both sides transcribed), confirm Vulkan backend on AMD GPU, then publish a higher version and confirm the installed app updates itself.

